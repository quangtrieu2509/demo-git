git init: tạo repo trên máy
git clone: lấy từ trên mạng về
git pull: đồng bộ từ trên mạng về máy

git add <.>: thêm file
git commit -m "<content_here>": xác nhận sửa
git push: đồng bộ từ máy lên mạng

git log: check trạng thái
git log - decorate - graph - oneline


git branch <branch_name>: thêm một branch
git brach: hiển thị các branch
git checkout <branch_name>: chuyển qua brach khác
git checkout -b <branch_name>: tạo branch rồi checkout luôn
git merge:

git rebase
git cherry-pick